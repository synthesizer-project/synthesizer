# pytest.ini
[pytest]
testpaths =
    tests
addopts = --ignore="tests/data/" --tb=long
# Outputs that don't respect the requested out_dtype are internal bugs, so
# fail the tests rather than just warning.
filterwarnings =
    error::synthesizer.utils.precision.InternalPrecisionWarning
